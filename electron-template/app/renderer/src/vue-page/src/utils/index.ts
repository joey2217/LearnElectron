export type MouseArea = "LEFT" | "MID" | "RIGHT" | null;

export function getMouseArea(): MouseArea {
  const w = document.documentElement.offsetWidth;
  const h = document.documentElement.offsetHeight;
  return null;
}
