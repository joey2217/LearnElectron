// import { ipcMain } from 'electron';

// export default class AppIpcMain {
//   handle(
//     channel: string,
//     listener: (event: Electron.IpcMainInvokeEvent, ...args: any[]) => any
//   ) {
//     console.log(`handle events ${channel}`);
//     ipcMain.handle(channel, listener);
//   }
// }
